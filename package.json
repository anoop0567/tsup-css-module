{
  "name": "tsup-css-module",
  "private": false,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/design-system & npm run dev --workspace=apps/demo",
    "build": "npm run build --workspaces"
  },
  "license": "MIT"
}
